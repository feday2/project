{% extends 'base.html.twig' %}

{% block body %}
<div>
{% if not userClient.supported %}
  {{userClient.updateAction.message}} 
  {% if userClient.updateAction.link %}
    </br>
    <a href="{{userClient.updateAction.link}}">Link to update</a>
  {% endif %}
{% else %}
    {% if timeToWork %}
      We are closed!</br>
      Time to open: {{ timeToWork.format('%d%')*24 + timeToWork.format('%h%')}}:{{ timeToWork.format('%I%') }}:{{ timeToWork.format('%S%') }}
    {% else %}
      {% if app.user %}
          ok
      {% else %}
          There <b>no char</b> for unregistered users
      {% endif %}
    {% endif %}
{% endif %}
</div>
{% endblock %}

{% block footer %}
    <div>
      {% if char is defined %}
        One character are above
      {% endif %}
    </div>
{% endblock %}