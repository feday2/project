<div>
    <div class="mb-3">
        {% if app.user %}
            You are logged in as {{ app.user.username }}, <a href="{{ path('logout') }}">Logout</a>
        {% else %}
            Please <a href="{{ path('login') }}">login</a> or <a href="{{ path('register') }}">register</a>
        {% endif %}
    </div>
</div>